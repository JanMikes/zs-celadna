<div data-controller="gallery" class="margin-component">
    <div class="row row-cols-6 g-3">
        {% for obrazek in data.Obrazek %}
            <div class="col {{ loop.index > data.Pocet_zobrazenych ? 'd-none' }}" data-gallery-target="item">
                <a href="{{ obrazek.Obrazek.large.url ? obrazek.Obrazek.large.url : obrazek.Obrazek.url }}" class="glightbox">
                    <img class="img-fluid" src="{{ obrazek.Obrazek.thumbnail ? obrazek.Obrazek.thubmnail.url : obrazek.Obrazek.url }}" alt="{{ obrazek.Popis }}" title="{{ obrazek.Popis }}">
                </a>
            </div>
        {% endfor %}
    </div>

    {% if data.Obrazek|length > data.Pocet_zobrazenych %}
        <button class="btn btn-primary mt-3" data-gallery-target="button" data-action="click->gallery#showMore">Zobrazit více</button>
    {% endif %}
</div>
