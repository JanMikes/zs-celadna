<div class="margin-component table-component table-responsive">
    <table class="table table-borderless">
        <thead>
            <tr>
                {% if data.Nadpis1 %}
                    <th>{{ data.Nadpis1 }}</th>
                {% endif %}
                {% if data.Nadpis2 %}
                    <th>{{ data.Nadpis2 }}</th>
                {% endif %}
                {% if data.Nadpis3 %}
                    <th>{{ data.Nadpis3 }}</th>
                {% endif %}
                {% if data.Nadpis4 %}
                    <th>{{ data.Nadpis4 }}</th>
                {% endif %}
            </tr>
        </thead>
        <tbody>
            {% for radek in data.Radky %}
                <tr>
                    {% if radek.Sloupec1 %}
                        <td class="{{ radek.Sloupec1.Styl == 'Zelené pozadí' ? 'green-cell' }}"><span>{{ radek.Sloupec1.Hodnota }}</span></td>
                    {% endif %}
                    {% if radek.Sloupec2 %}
                        <td class="{{ radek.Sloupec2.Styl == 'Zelené pozadí' ? 'green-cell' }}"><span>{{ radek.Sloupec2.Hodnota }}</span></td>
                    {% endif %}
                    {% if radek.Sloupec3 %}
                        <td class="{{ radek.Sloupec3.Styl == 'Zelené pozadí' ? 'green-cell' }}"><span>{{ radek.Sloupec3.Hodnota }}</span></td>
                    {% endif %}
                    {% if radek.Sloupec4 %}
                        <td class="{{ radek.Sloupec4.Styl == 'Zelené pozadí' ? 'green-cell' }}"><span>{{ radek.Sloupec4.Hodnota }}</span></td>
                    {% endif %}
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
