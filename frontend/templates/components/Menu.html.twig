{% for menuItem in this.getItems(type) %}
    <li class="nav-item">
        <twig:Odkaz class="nav-link px-2" data="{{ menuItem.Odkaz }}">
            {{ menuItem.Nadpis }}
        </twig:Odkaz>
    </li>
{% endfor %}
